apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: eks-demo
spec:
  type: LoadBalancer
  selector:
    app: eks-postgres-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000